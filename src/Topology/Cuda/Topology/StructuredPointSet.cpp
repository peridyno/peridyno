#include "StructuredPointSet.h"

namespace dyno
{
	template<typename Coord>
	StructuredPointSet<Coord>::StructuredPointSet()
	{
	}

	template<typename Coord>
	StructuredPointSet<Coord>::~StructuredPointSet()
	{
	}

	DEFINE_CLASS(StructuredPointSet);
}